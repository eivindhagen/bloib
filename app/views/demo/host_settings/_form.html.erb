<%= form_for(@demo_host_setting) do |f| %>
  <% if @demo_host_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demo_host_setting.errors.count, "error") %> prohibited this demo_host_setting from being saved:</h2>

      <ul>
      <% @demo_host_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key %><br />
    <%= f.text_field :key %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
