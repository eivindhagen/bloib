<%= form_for(@demo_site_url) do |f| %>
  <% if @demo_site_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demo_site_url.errors.count, "error") %> prohibited this demo_site_url from being saved:</h2>

      <ul>
      <% @demo_site_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
